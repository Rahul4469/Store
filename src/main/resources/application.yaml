spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: Rax@7692*#Rc
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

stripe:
  apiUrl : https://api.stripe.com
  enabled : true
  timeout : 1000
  supported-currencies : USD, Rupees
payment-gateway: stripe